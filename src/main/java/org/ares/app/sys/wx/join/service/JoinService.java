package org.ares.app.sys.wx.join.service;

/**
 * access db
 * use @Inject inject,don't need config in xml
 * @author ly
 *
 */
public interface JoinService {

	String getContent(String content);
	
}
